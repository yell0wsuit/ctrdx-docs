---
title: Class GLenum
description: Defines OpenGL-style capability and state constants used by the renderer abstraction layer. Values are sequential internal IDs, not actual OpenGL enum values.
---

# Class GLenum

Namespace: `CutTheRopeDX.Desktop`

Assembly: `CutTheRope-DX.dll`

Source: `GLenum.cs`

Defines OpenGL-style capability and state constants used by the renderer abstraction layer. Values are sequential internal IDs, not actual OpenGL enum values.

## Declaration

```csharp
public class GLenum
```

## Inheritance

- [object](https://learn.microsoft.com/en-us/dotnet/api/system.object?view=net-10.0)
- [GLenum](GLenum.md)

## Fields

### GL_ARRAY_BUFFER {#f-cuttheropedx-desktop-glenum-gl-array-buffer}

```csharp
internal const int GL_ARRAY_BUFFER
```

Array buffer target for vertex data.

### GL_BLEND {#f-cuttheropedx-desktop-glenum-gl-blend}

```csharp
internal const int GL_BLEND
```

Alpha blending capability.

### GL_COLOR {#f-cuttheropedx-desktop-glenum-gl-color}

```csharp
internal const int GL_COLOR
```

Color matrix mode.

### GL_COLOR_ARRAY {#f-cuttheropedx-desktop-glenum-gl-color-array}

```csharp
internal const int GL_COLOR_ARRAY
```

Color array client state.

### GL_DYNAMIC_DRAW {#f-cuttheropedx-desktop-glenum-gl-dynamic-draw}

```csharp
internal const int GL_DYNAMIC_DRAW
```

Dynamic draw usage hint for buffer data.

### GL_FLOAT {#f-cuttheropedx-desktop-glenum-gl-float}

```csharp
internal const int GL_FLOAT
```

Float data type identifier.

### GL_LINE_STRIP {#f-cuttheropedx-desktop-glenum-gl-line-strip}

```csharp
internal const int GL_LINE_STRIP
```

Line strip primitive type.

### GL_MODELVIEW {#f-cuttheropedx-desktop-glenum-gl-modelview}

```csharp
internal const int GL_MODELVIEW
```

Model-view matrix mode.

### GL_POINTS {#f-cuttheropedx-desktop-glenum-gl-points}

```csharp
internal const int GL_POINTS
```

Point list primitive type.

### GL_PROJECTION {#f-cuttheropedx-desktop-glenum-gl-projection}

```csharp
internal const int GL_PROJECTION
```

Projection matrix mode.

### GL_SCISSOR_TEST {#f-cuttheropedx-desktop-glenum-gl-scissor-test}

```csharp
internal const int GL_SCISSOR_TEST
```

Scissor test capability for clipping.

### GL_TEXTURE {#f-cuttheropedx-desktop-glenum-gl-texture}

```csharp
internal const int GL_TEXTURE
```

Texture matrix mode.

### GL_TEXTURE_2D {#f-cuttheropedx-desktop-glenum-gl-texture-2d}

```csharp
internal const int GL_TEXTURE_2D
```

2D texture capability.

### GL_TEXTURE_COORD_ARRAY {#f-cuttheropedx-desktop-glenum-gl-texture-coord-array}

```csharp
internal const int GL_TEXTURE_COORD_ARRAY
```

Texture coordinate array client state.

### GL_TRIANGLES {#f-cuttheropedx-desktop-glenum-gl-triangles}

```csharp
internal const int GL_TRIANGLES
```

Triangle list primitive type.

### GL_TRIANGLE_STRIP {#f-cuttheropedx-desktop-glenum-gl-triangle-strip}

```csharp
internal const int GL_TRIANGLE_STRIP
```

Triangle strip primitive type.

### GL_UNSIGNED_SHORT {#f-cuttheropedx-desktop-glenum-gl-unsigned-short}

```csharp
internal const int GL_UNSIGNED_SHORT
```

Unsigned short data type identifier.

### GL_VERTEX_ARRAY {#f-cuttheropedx-desktop-glenum-gl-vertex-array}

```csharp
internal const int GL_VERTEX_ARRAY
```

Vertex array client state.

